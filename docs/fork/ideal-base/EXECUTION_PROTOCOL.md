# Graph-first execution protocol

Archived on 2026-08-08.

This file preserves the final operating rules that governed the ideal-base
program. The program is complete, so the live command paths, bootstrap steps,
and runnable workflow instructions have been retired.

## What remains authoritative

- `README.md` for the archive index.
- `BASELINE.md` for the protected starting boundary.
- `ACCEPTANCE_STANDARD.md` for the accepted result and support limits.
- `DECISIONS.md` for final decisions and reopen triggers.
- `STATE.json` and `WORK_GRAPH.json` for the frozen record.

## Historical record only

The former graph-first workflow, evidence contract, and recovery rules are kept
as history. They are no longer used as current execution instructions.
