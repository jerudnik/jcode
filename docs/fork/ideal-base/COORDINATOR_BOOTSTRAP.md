# Fresh coordinator bootstrap

Archived on 2026-08-08.

This was the starting prompt for the live ideal-base workflow. It is preserved
only as historical context. For the completed program, start from
`README.md`, `ACCEPTANCE_STANDARD.md`, `BASELINE.md`, `DECISIONS.md`,
`STATE.json`, and `WORK_GRAPH.json` instead.
