# Independent Sol signoff: R01 and R02 seam ledgers

Date: 2026-07-15 UTC  
Reviewer posture: read-only verification. No repository files, refs, stashes, worktrees, credentials, network, live daemon, or external service were changed or used. I intentionally did not rerun cargo tests because doing so would write build artifacts under the target worktrees. I instead reproduced decisive read-only Git/source commands and verified the stated test names and preserved review outputs.

## Executive verdict

| Seam commit | Verdict | Integration-ready? | Pilot-ready? | Confidence |
|---|---|---:|---:|---|
| R01 `b25e902eed532156c54ca14f7da6b8e780bde234` in `/Users/jrudnik/labs/jcode-seam-r01` | PASS with IMPORTANT pilot blocker | YES, for docs/adjudication integration | NO | High for commit integrity and R01 disposition, medium-high for runtime boundary completeness |
| R02 `3217dbcbf22ea6ef13525e7f3f1571b0a49132d6` in `/Users/jrudnik/labs/jcode-seam-r02` | PASS with IMPORTANT pilot blockers | YES, for docs/adjudication integration | NO | High for commit integrity and stale-cache finding, medium-high for compose disposition |

No CRITICAL finding blocks integrating either documentation-only seam commit into the recovery branch. Both commits remain explicitly blocked for Phase 3 pilot behavior until their ledgers' named fixture/test prerequisites are satisfied.

## Ten decisive checkpoints, narrowed and exhausted

1. **Commit identity, cleanliness, and exact changed paths.**
   - `git -C /Users/jrudnik/labs/jcode-seam-r01 rev-parse HEAD` returned `b25e902eed532156c54ca14f7da6b8e780bde234`.
   - `git -C /Users/jrudnik/labs/jcode-seam-r02 rev-parse HEAD` returned `3217dbcbf22ea6ef13525e7f3f1571b0a49132d6`.
   - `git status --short` printed no entries for both target repos.
   - `git diff-tree --no-commit-id --name-status -r HEAD` showed exactly:
     - R01: `A docs/fork/recovery/seams/R01-runtime-build-identity/{grok-review.md,ledger.md,opus-review.md}`.
     - R02: `A docs/fork/recovery/seams/R02-config-provider-routing/{grok-review.md,ledger.md,opus-review.md}`.

2. **R01 fixed refs and required fork/upstream `paths.rs` divergence.**
   - `git rev-parse --verify` resolved fork `7ff4fc6be8dcf0410f2f61994752fdf5ee93e6e4`, upstream `802f6909825809e882d9c2d575b7e478dce57d3b`, and base `631935dd1d3b2e31e167e2b12ad463e54bcf4b8d`.
   - `git merge-base 7ff4fc6be8dcf0410f2f61994752fdf5ee93e6e4 802f6909825809e882d9c2d575b7e478dce57d3b` returned the base `631935dd1d3b2e31e167e2b12ad463e54bcf4b8d`.
   - `git diff --numstat base upstream -- crates/jcode-build-support/src/paths.rs` was empty and `git diff --unified=0 base upstream -- ... | wc -c` returned `0`.
   - `git diff --numstat base fork -- crates/jcode-build-support/src/paths.rs` returned `333 1 crates/jcode-build-support/src/paths.rs`.
   - This independently confirms the ledger's line 81 claim and the R01 disposition foundation: no post-base upstream `paths.rs` policy delta, material fork-only reload-target authority delta.

3. **R01 preserved review hashes and external stale-daemon incident evidence.**
   - R01 `opus-review.md` SHA-256: `21918fd79db7a7a9e7360a68699b6283e272ed56a359f912a0a0940d529d1e60`; `cmp -s /tmp/jcode-r01-opus-review.md .../opus-review.md` was identical.
   - R01 `grok-review.md` SHA-256: `9cb7beb3304074056ad1f2cbcdcbfa1f4293b5c2694d63fe180249e3e86d40b6`; `cmp -s /tmp/jcode-r01-grok-review.md .../grok-review.md` was identical.
   - External incident note hash reproduced as `80012e2ce61c578c943263b944bbaca27ac7dbd440af50c1f21f6e0291d8f1a9` for `/Users/jrudnik/notes/projects/jcode/maintenance/bug-server-reload-stale-daemon-version-check.md`.
   - These match R01 ledger lines 18-24.

4. **R01 authority-today implementation evidence and projection boundary.**
   - `git grep` at fork baseline found `nix_managed_launcher_override` in `crates/jcode-build-support/src/paths.rs:545`, and calls from `client_update_candidate` at `:565`, `shared_server_update_candidate` at `:603`, and `preferred_reload_candidate` at `:698`.
   - Non-forced reload stays directional: `crates/jcode-app-core/src/server/client_session.rs:720` has `if !force && !super::server_has_newer_binary()`.
   - Canonical identity is tuple-like, not one string: `crates/jcode-build-meta/src/lib.rs:11` has `GIT_HASH`, `:32` has `BUILD_SOURCE_DIR`; `crates/jcode-selfdev-types/src/lib.rs:74-88` has `SourceState` including `version_label` and `source_fingerprint`.
   - Projection boundary evidence: `wire.rs:227,232` carries `protocol_version` and `build_hash`, while restart snapshots carry `jcode_version`, optional `jcode_git_hash`, optional `jcode_git_dirty`, and `is_selfdev` at `jcode-session-types/src/lib.rs:195-201`.
   - This supports R01 ledger lines 33-42 and 83-90: fork authority today, but pilot blocked until R03A/R04 projections are explicitly proven or typed as compatibility-only.

5. **R01 stated tests verified without running builds.**
   - Preserved Opus review states the build-support suite passed and names stale/reload repair tests at lines 61-71.
   - Preserved Grok review names decisive test commands and pilot gaps at lines 112-160.
   - Read-only `git grep` verified definitions at fork baseline, including `dirty_source_state_uses_fingerprint_in_version_label` in `crates/jcode-build-support/src/tests.rs:292`, `pending_activation_can_complete_and_roll_back` at `:307`, `normal_shared_server_candidate_repairs_stale_shared_channel_to_stable` at `:389`, `update_leaves_daemon_reload_target_stale_when_shared_server_pinned_to_selfdev` at `:611`, and app-core reload candidate test modules at `crates/jcode-app-core/src/server/util.rs:560` and `:771`.
   - I did not rerun these tests to honor the no-worktree-mutation constraint.

6. **R02 preserved review hashes, `cmp`, and explicit hard-break exception.**
   - R02 `opus-review.md` SHA-256: `122b647b2cbd39d64c43d4dc07a89edf83d926a03ab175318945c9b32d51be8f`; `cmp -s /tmp/jcode-r02-opus-review.md .../opus-review.md` was identical.
   - R02 `grok-review.md` SHA-256: `8a6c18e5f99862e92155160da44ae9c2e0ef3f0b00320a573b546df2fa7d4312`; `cmp -s /tmp/jcode-r02-grok-review.md .../grok-review.md` was identical.
   - `perl -ne 'print $. . ":" . $_ if /  $/' .../grok-review.md` found exactly lines 3-7 with two trailing spaces.
   - `git diff --check HEAD^ HEAD -- .../grok-review.md` reported exactly those five trailing whitespace entries, and `git -c core.whitespace=-blank-at-eol diff --check ...` was clean.
   - R02 ledger line 146 documents this exact exception. I accept the exception because hash and `cmp` prove verbatim identity and the ledger documents it.

7. **R02 fixed refs and two-sided `catalog_routes.rs` composition evidence.**
   - The same fixed fork/upstream/base refs resolved, and their merge base was `631935dd1d3b2e31e167e2b12ad463e54bcf4b8d`.
   - `git diff --numstat base fork -- crates/jcode-base/src/provider/catalog_routes.rs` returned `96 14`.
   - `git diff --numstat base upstream -- crates/jcode-base/src/provider/catalog_routes.rs` returned `75 4`.
   - Upstream has definitive OpenRouter fallback suppression at `catalog_routes.rs:705` and `:722` via `standard_catalog_lists_model(&or_model) != Some(false)`.
   - Fork has provider/profile route identity and credential-source behavior, including `resolve_current_model_spec` references at `:13`, `:33`, `:88`, and `ApiKeyCredentialSource::primary_only` at `:209`; it also already has fallback suppressor lines at `:710` and `:727`.
   - This supports R02 ledger lines 53 and 70-74: compose, not pick one side.

8. **R02 required stale cached tier control flow reproduced.**
   - Fork baseline `subscription_catalog.rs:14-18` has only `Plus` and `Flagship` tiers; `JcodeTier::parse` recognizes only `plus` and `flagship` at `:52-58`.
   - `effective_tier()` falls back to `Plus` at `subscription_catalog.rs:168-170`, and `cached_tier()` parses persisted `JCODE_TIER` at `:173-177`.
   - `fetch_subscription_me()` parses a successful live response, then stores cache only under `if let Some(tier) = me.parsed_tier()` at `subscription_api.rs:76-85`; an unknown successful tier therefore leaves an existing cached `Flagship` untouched.
   - The current test `subscription_me_tolerates_missing_resets_at_and_unknown_tier` accepts `"tier": "mystery"` and asserts `parsed_tier() == None` at `subscription_api.rs:118-129`.
   - This reproduces the decisive R02 blocker in ledger lines 51, 60, 72, 77, 89-96, and 138.

9. **R02 stated tests and local admission/usage consumers verified without running builds.**
   - Preserved Opus review states `nix develop --command cargo test -p jcode-base --lib subscription_catalog` passed 10 tests at lines 10-16.
   - Preserved Grok review states warmed `subscription_catalog::tests` and `subscription_api::tests` passed, but also says no test covers stale cached Flagship plus unknown live tier at lines 51, 68, 126, 144-146.
   - Read-only `git grep` verified definitions at fork baseline: `subscription_me_parses_expected_shape` at `subscription_api.rs:95`, `subscription_me_tolerates_missing_resets_at_and_unknown_tier` at `:118`, `tier_parse_round_trips` at `subscription_catalog.rs:306`, `tier_gating_orders_plus_below_flagship` at `:316`, `flagship_models_are_gated_above_plus` at `:324`, `effective_tier_defaults_to_plus_when_unknown` at `:336`, and `test_filtered_display_models_respects_curated_subscription_catalog` at `provider/tests/catalog_subscription.rs:260`.
   - `git grep -E 'tier\.allows|\.min_tier|effective_tier\('` found admission/display consumers in `provider/models.rs:191-192`, `subscription_catalog.rs:168,192`, and TUI auth rendering at `tui/app/auth.rs:116`.
   - `git grep -E 'used_usd|budget_usd'` showed usage parsing/display in `subscription_api.rs` and TUI auth rendering, not a local over-budget router admission gate.

10. **R00/R09/R11 compliance, disposition logic, boundaries, slices, and negative findings.**
   - R00 overlay binds fixed refs, no stash replay, no prompt mutation, and explicit rollback/stop budgets: lines 26-31 and 56-58.
   - R09 overlay forbids blanket `--update`, keeps red debt visible, and requires behavior-seam debt ownership: lines 24-30 and 52-57.
   - R11 overlay requires append-only recovery truth, hashed external evidence, user-controlled preserved prompt edit, and ownership boundaries: lines 24-29 and 47-52.
   - R01 ledger obeys this shape: scope/invariants at lines 26-31, negative findings at lines 67-72, adjudication at lines 74-81, recommendation and authority boundaries at lines 83-90, bounded slices with rollback/stop conditions at lines 93-100, R09 debt handling at lines 102-109, validation/signoff at lines 110-119.
   - R02 ledger obeys this shape: scope/invariants at lines 24-30, negative findings/gaps at lines 57-64, adjudication at lines 66-78, authority/pilot verdict at lines 79-96, fixture contract at lines 98-112, bounded slices with rollback/stop conditions at lines 132-141, R09 debt ownership at lines 121-130, validation/signoff at lines 143-154.

## Per-seam findings

### R01 runtime build identity and reload authority

**PASS**
- The commit is docs-only and exactly scoped to the R01 seam directory.
- Review preservation is byte-identical to the designated `/tmp` artifacts, and the stale-daemon incident note hash reproduces.
- The fixed-ref comparison is valid. The required `paths.rs` divergence is decisive: upstream has zero post-base diff and fork has a `333/1` delta.
- Disposition logic is sound: `retain-fork`, authority today `fork`. Upstream may be R04/R03A candidate evidence, but it does not supply the R01 reload-target authority delta.
- R00/R09/R11 obligations are present: fixed refs, no stash replay, no source implementation, visible debt, verbatim preserved reviews, and bounded slices with stop/rollback conditions.

**IMPORTANT**
- Pilot entry remains blocked. R01 identity is a tuple, while R03A/R04 projections are not yet proven to carry equivalent identity for dirty same-commit builds. The ledger correctly identifies `build_hash` as a compatibility projection rather than full canonical identity.
- The preserved reviews report tests, and test definitions exist, but I did not rerun them because that would write build outputs. This is a verification limitation, not a ledger defect.

**CRITICAL**
- None found.

**Integration readiness**
- Integration-ready for recovery documentation/adjudication: **YES**.
- Integration-ready as behavioral/pilot authority without additional fixture work: **NO**.

### R02 configuration, provider routing, auth readiness, entitlement

**PASS**
- The commit is docs-only and exactly scoped to the R02 seam directory.
- Review preservation is byte-identical to the designated `/tmp` artifacts.
- The Grok five-line two-space Markdown hard-break exception is acceptable because `shasum`, `cmp`, default `git diff --check`, and ignored-blank-at-EOL `diff --check` all match the ledger's documented exception.
- Disposition logic is sound: `compose`, authority today `split`. Retain fork provenance/credential/route identity behavior, compose `catalog_routes` fallback behavior, and treat upstream's richer catalog as candidate schema only until product-owned fixtures decide it.
- The stale cached tier blocker is real and reproduced from source: a successful unknown `/v1/me` tier takes the `None` branch and does not clear a stronger cached tier.
- R00/R09/R11 obligations are present: no patch-equivalence claim, no stash replay, R02-owned red-debt entries enumerated, hashed/verbatim evidence, bounded slices with stop/rollback conditions.

**IMPORTANT**
- Pilot entry remains blocked until the no-secret fixture proves known and unknown `/v1/me` tier behavior, stale Flagship cannot admit Flagship-only models, product-owned tier truth is recorded, `set_model` and display/filter admission agree, and env provenance is handled or disabled in the fixture.
- Upstream's five-tier prices, budgets, labels, and model floors are not product authority. Importing them mechanically would be an integration error.
- The preserved reviews report relevant tests, and test definitions exist, but the decisive missing stale-cache regression is absent by design and called out by the ledger.

**CRITICAL**
- None found.

**Integration readiness**
- Integration-ready for recovery documentation/adjudication: **YES**.
- Integration-ready as behavioral/pilot authority without additional fixture work: **NO**.

## Negative findings confirmed

- No exact patch-ID or upstream-equivalence claim is being smuggled into either seam.
- No repository, ref, stash, worktree, credential, network, live daemon, or external service was used or mutated during my signoff.
- R01 has no evidence that a live R03A subscriber, reload daemon, manifest/channel markers, and R04 restart record already agree for two dirty builds from the same commit.
- R02 has no evidence that successful unknown live tier clears or downgrades a stale cached Flagship entitlement.
- R02 has no product authority for upstream tier/pricing/budget/model-floor constants.
- Both ledgers correctly separate docs/sync/fix/refactor slices and name rollback or stop conditions rather than authorizing broad implementation.

## Final signoff

I sign off both commits as integration-ready documentation/adjudication commits with IMPORTANT pilot blockers preserved. I do not sign off either seam as pilot-ready or implementation-complete.
