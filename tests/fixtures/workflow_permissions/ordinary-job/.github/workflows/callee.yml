on:
  workflow_call:
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo build
  cleanup:
    permissions:
      contents: read
      actions: write
    runs-on: ubuntu-latest
    steps:
      - run: echo cleanup
