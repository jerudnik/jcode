{
  "_comment": [
    "Authoritative build matrix for .github/workflows/nix.yml.",
    "tests/test_nix_distribution_policy.py parses the workflow and this file",
    "to assert that each event mode uses the declared system and runner, and that",
    "platform support advertised in docs/NIX.md matches the systems CI actually",
    "builds. Any flake system absent from every list here is unverified and must",
    "be documented best-effort.",
    "Adding a system here without adding a docs row (or vice versa) fails that test."
  ],
  "pull_request": [
    { "system": "x86_64-linux", "runner": "ubuntu-latest" }
  ],
  "default": [
    { "system": "x86_64-linux", "runner": "ubuntu-latest" },
    { "system": "aarch64-darwin", "runner": "macos-latest" }
  ]
}
