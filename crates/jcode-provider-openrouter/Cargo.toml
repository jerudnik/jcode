[package]
name = "jcode-provider-openrouter"
version = "0.1.0"
edition = "2024"

[dependencies]
dirs = "5"
jcode-cache-isolation = { path = "../jcode-cache-isolation" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
